{
  "name": "@metapredict/smart-contracts",
  "version": "1.0.0",
  "description": "MetaPredict.ai Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network opbnb-testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network opbnb-mainnet",
    "verify": "hardhat verify --network",
    "coverage": "hardhat coverage"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.8.0",
    "@openzeppelin/contracts": "^5.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/node": "^20.0.0",
    "hardhat": "^2.19.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  }
}

